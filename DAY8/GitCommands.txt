git clone 
// to clone repo from any git flavour
git add . 
// to add file to git tree
git commit -m "msg" 
// to provide message & to commit our changes

git push origin 
// to push changes into our git tree & github(flavour).

git pull 
// to fetch changes from github(any flavour).

git fetch origin
// to fetch branches from github.